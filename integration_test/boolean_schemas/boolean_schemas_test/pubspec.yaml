name: boolean_schemas_test
description: A test suite for the boolean schemas API.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  boolean_schemas_api:
    path: ../boolean_schemas_api
  path: ^1.9.1
  tonik_util: ^0.9.0

dev_dependencies:
  test: ^1.32.0
  test_helpers:
    path: ../../test_helpers
  very_good_analysis: ^11.0.0

dependency_overrides:
  tonik_util:
    path: ../../../packages/tonik_util
