name: test_helpers
description: Shared test utilities for integration tests
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.13.0 <4.0.0'

dependencies:
  dio: ^5.11.1
  http: ^1.6.0
  path: ^1.9.1
  test: ^1.32.0
  tonik_util: ^0.9.0

dependency_overrides:
  tonik_util:
    path: ../../packages/tonik_util
