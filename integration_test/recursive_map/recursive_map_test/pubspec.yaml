name: recursive_map_test
description: Integration tests for recursive map and list typedef shapes.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.13.0 <4.0.0'

dependencies:
  path: ^1.9.1
  recursive_map_api:
    path: ../recursive_map_api
  tonik_util: ^0.9.0

dev_dependencies:
  test: ^1.32.0
  test_helpers:
    path: ../../test_helpers
  very_good_analysis: ^11.0.0

dependency_overrides:
  tonik_util:
    path: ../../../packages/tonik_util
