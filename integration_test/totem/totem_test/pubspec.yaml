name: totem_test
description: Integration tests for totem API code generation
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  path: ^1.9.1
  tonik_util: ^0.9.0
  totem_api:
    path: ../totem_api

dev_dependencies:
  test: ^1.32.0
  test_helpers:
    path: ../../test_helpers
  very_good_analysis: ^11.0.0

dependency_overrides:
  tonik_util:
    path: ../../../packages/tonik_util
